{
  "name": "@postman/wormhole",
  "version": "1.0.0-alpha.1",
  "description": "A custom network layer for Postman.",
  "main": "index.js",
  "directories": {
    "test": "package_tests"
  },
  "scripts": {
    "build": "node-gyp rebuild",
    "clean": "node-gyp clean",
    "test": "mocha package_tests",
    "cmake:build": "cmake -S . -B native_build && cmake --build native_build",
    "cmake:format": "cmake --build native_build --target clang_format",
    "cmake:test": "./native_build/test/request_unit_test",
    "cmake:all": "npm run cmake:build && npm run cmake:format && npm run cmake:test",
    "cmake:clean": "rm -rf native_build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/postmanlabs/wormhole.git"
  },
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/postmanlabs/wormhole/issues"
  },
  "homepage": "https://github.com/postmanlabs/wormhole#readme",
  "devDependencies": {
    "mocha": "^10.2.0",
    "node-gyp": "^9.3.1"
  },
  "dependencies": {
    "node-addon-api": "^6.0.0"
  }
}
